// swift-tools-version:5.5
import PackageDescription

let package = Package(
    name: "GameApp",
    targets: [
        .executableTarget(
            name: "GameApp",
            path: "GameApp"
        )
    ]
)
